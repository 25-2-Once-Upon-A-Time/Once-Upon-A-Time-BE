spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/db25308?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: dbid253
    password: dbpass253
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
    show-sql: true

server:
  port: 8080

jwt:
  token:
    secret: "dev-secret-key-1234"